/**
 * 指令包
 * <p>
 * 常量（constants）指令
 * 加载（loads）指令
 * 存储（stores）指令
 * 操作数栈（stack）指令
 * 数学（math）指令
 * 转换（conversions）指令
 * 比较（comparisons）指令
 * 控制（control）指令
 * 引用（references）指令
 * 扩展（extended）指令
 * 保留（reserved）指令
 */
package xianzhan.jvmjava.java.instruction;